[package]
name = "mavros-vm"
version = "0.1.0"
edition = "2024"

[dependencies]
ark-ff = { version = "0.5.0" }
mavros-artifacts = { path = "../mavros-artifacts" }
opcode-gen = { path = "../opcode-gen" }
tracing = "0.1.41"
